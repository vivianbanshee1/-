<!-- Slide number: 1 -->

大作业概要设计要求
软件与理论中心
张艳梅

![地球](Picture5.jpg)

<!-- Slide number: 2 -->
3.概要设计要点
概要设计主要从以下5个方面考虑：
用户界面：界面友好，要能从界面提示信息了解大作业各部分的状态。
自动机模型：主体对象的复杂行为过程可以用自动机模型来描述。绘制状态迁移图，图上需要描述引起状态迁移的条件，并且要在文档中附加说明进入某状态要做的动作。
重点数据：较全面地给出各个函数要共享的数据。
程序模块化：函数接口说明，函数调用关系说明。
关键算法：如使用概率模型，如正态分布、泊松分布等，模拟生成乘客数量的算法。
2

<!-- Slide number: 3 -->
用户界面设计原则（1）
1.置用户于控制之下
提供灵活的交互、允许用户交互可以被中断和撤销……
2. 减少用户的记忆负担
减少对短期记忆的要求、建立有意义的缺省、以不断进展的方式提示信息……
3. 保持界面一致
3

<!-- Slide number: 4 -->
用户界面设计原则（2）
4. 输入和输出的方式和格式应当尽可能方便用户的使用
（1）对所有的输入数据都进行检验，从而识别错误的输入，以保证每个数据的有效性。
（2）检查输入项的各种重要组合的合理性，必要时报告输入状态信息。
（3）使得输入的步骤和操作尽可能简单，并保持简单的输入格式。
（4）输入数据时，应允许使用自由格式输入。
（5）应允许缺省值。
（6）输入一批数据时，最好使用输入结束标志，而不要由用户指定输入数据数目。
（7）在以交互式输入／输出方式进行输入时，要在屏幕上使用提示符明确提示交互输入的请求，指明可使用选择项的种类和取值范围。同时，在数据输入的过程中和输入结束时，也要在屏幕上给出状态信息。
（8）给所有的输出加注解，说明输出数据的含义。

4

### Notes:
输入时：
数据之间的分隔符
数据允许的范围
数据的单位（如厘米还是米）
允许使用缺省值

<!-- Slide number: 5 -->
用户界面设计原则（3）
5.错误处理
（1）返回错误代码。返回的错误代码指示了错误发生的原因，调用者可以根据错误代码进行错误处理。
（2）调用错误处理函数。用错误处理函数对错误进行统一处理，这样有利于集中地对错误进行管理。
（3）显示错误信息。当错误发生时，提示错误信息。例如当用户输入了非法数据时，向用户提示正确的输入格式。
（4）记录日志。当错误发生时，记录系统日志文件，并继续执行。
（5）退出程序。这种方式对一些安全性要求较高的程序比较合适，防止继续操作可能对系统带来的破坏

5

<!-- Slide number: 6 -->
OJ版本输入：裁判程序输入（1）
OJ版输入格式：四类输入.
（1）初始地图：21行，其中前20行每行固定为长度为20的字符串（格式如下），代表20×20的初始地图及游戏的初始状态，第21行为一个正整数代表N。
地图网格（行号 0~19，列号 0~19，左上角坐标为0 0）字符含义如下：
`#` ：墙，地图边缘，不可穿越；
`.` ：空地，可移动区域，无元素 ；
`H`： 蛇头 ，蛇的头部位置 ；
`B` ：蛇身 ，蛇的身体节段 ；
`F` ：食物 ，可被蛇吃掉并增长身体的元素；
`O`：障碍物 ，固定不可移动，碰撞后游戏结束；
6

<!-- Slide number: 7 -->
OJ版本输入：裁判程序输入（2）
OJ版输入格式：三类输入.
（2）食物指令：单独一行，两个整数以空格分隔。两个整数均大于0且小于19，代表新食物的行号和列号。

（3）平安无事指令：单独一行，两个整数以空格分隔。两个整数均为20，代表无碰撞、无新食物。

（4）死亡判定：单独一行，两个整数以空格分隔。两个整数均为100，表示玩家的移动方案已导致游戏结束（蛇身碰撞），程序应直接退出。
7

<!-- Slide number: 8 -->
# OJ版本输出：移动指令和得分
每一次接收裁判指令后，程序需要输出2部分内容：
（1）玩家操作：单独一行。一个字符，即‘W’、‘A’、‘S’和‘D’之一，代表蛇移动的向上、左、下和右方向值。
（2）玩家操作前的累计得分：一个整数，单独一行。
程序还需要隐藏记录当前的游戏空间状态， 20行×20列的二维空间，记录格子被不同形状的蛇身占据或空置。
调试程序时，尤其需要游戏空间的显示提示，可以用字符图形辅助显示。
8

<!-- Slide number: 9 -->
# 图形界面设计要求
要求：说明所画界面中对应的各种操作含义和预期效果。

![](图片11.jpg)

![](图片14.jpg)

<!-- Slide number: 10 -->
# 状态机设计要求
状态图要细化完善，使之配合后续的控制策略算法。
要求说明每个状态、动作和事件的内容和含义。
状态机不要过于简单，将复杂控制的难题交给算法部分。
也不要把状态机作的太复杂，不要把算法步骤也设计为状态。

<!-- Slide number: 11 -->
需求分析示例：OJ版贪吃蛇游戏

OJ版贪吃蛇游戏的状态机模拟

游戏空间 初始化
初始
随机防置障碍物
摆放贪吃蛇的蛇身
结束
游戏空间记录

游戏结束提示
食物掉落

移动策略
移动指令
确认碰撞
碰撞检测
蛇身增长
得分计算
11

<!-- Slide number: 12 -->
# 数据结构设计问题
要求: 自定义类型的结构定义，要完整并有文字注释.
在实现贪吃蛇游戏时，蛇身的动态增长和移动是核心功能。为了高效地管理蛇身的节点，需要选择适合的线性表数据结构。

<!-- Slide number: 13 -->
# 高层数据结构设计:关键数据
贪吃蛇状态数据设计：
typedef struct {
	int x,y;
}Position;
typedef struct {
	Position body[MAX_LEN];
	int flag;//标记蛇头、蛇身和尾部
	int direct;//当前移动方向
	int len;//蛇身长度
}Snake;
13

<!-- Slide number: 14 -->
# 高层数据结构设计:关键数据
贪吃蛇游戏空间设计：
二维数组20*20，
格子状态：空白、食物、障碍、占据。

14

<!-- Slide number: 15 -->
# 高层数据结构设计:和算法函数的接口

游戏规则模块和数据结构的关系要理清：
仅占据可移动的游戏空间；（算法读取数据）
不能反向移动，移动时不能碰自己；（规则判定）
初始蛇头默认朝上，蛇身长度3格；（数据初始化）
吃到食物长1节，移动N次长1节，两个规则不同时叠加。（增长规则的叠加判定）
15

<!-- Slide number: 16 -->
# 高层数据结构设计: 常量
尽量不使用全局变量！
常量、符号常量（宏定义）可以有。
设计原则：尽量不要将常量数值写死在代码里。
#define PARA_FNAME “dict.dic”  //配置文件名
#define MAX_STATIONS 10 //最大车站数
#define MAX_DUR 6		//最大车站间距
#define IDLE 0		//公交车空闲状态
…

思考：当前方向变量，和对应的字符常量，如何设计？
状态常量、字符数组…
16

<!-- Slide number: 17 -->
# 程序模块化
程序的模块结构，即整个程序分成哪几个相对独立的代码模块？
要考虑第二版本的问题：如何设计模块，使得当增加一个新的CPU线程时，对现有代码的修改尽量少？
主模块
main.c
输入
模块
输出模块
算法模块
规则
模块
input.c
rule.c
output.c
algorithm.c
17

<!-- Slide number: 18 -->
# 功能模块划分：贪吃蛇游戏实例
主控
输入
随机模块模拟
读取键盘指令
输出
累计得分输出
游戏空间输出
蛇身移动输出
操作提示
策略算法
设置参数N
一个食物移动策略
多食物移动策略
状态机(规则)
输赢判断
蛇身位置
得分计算
游戏空间记录
18

<!-- Slide number: 19 -->
# 单线程模式的主体结构
int main(void){
    setParam();/*设置参数*/
    init(); /*初始化*/
    int cmd=getCmd(); /*读指令、分析指令*/
    int sec=0;
    while (cmd!=END || state != END){
	sec++;
	stateRun(); /*状态机*/
	playAlg();/*调用算法*/
	gameRule();/*游戏规则判定*/
	outInfo();/*输出信息*/
	cmd=getCmd();
    }
}
19

<!-- Slide number: 20 -->
# 模块设计问题
要求：清楚说明模块划分、线程划分,以及模块间调用关系。

错误1：有小组把模块关系图画成了函数调用图。
错误2：动画版本概要设计，线程间存在函数调用。例：线程A调用线程2的函数。
A.c

C.c

B.c
D.c

<!-- Slide number: 21 -->
# 示例：函数功能说明
要求2：清楚说明模块中主要函数的原型和功能说明。
| 函数声明 | 功能 |
| --- | --- |
| AskForDown(LayerNumber: int) | //一个新的下楼（Downstairs）的呼叫请求到达， //当用户在任何一个楼层（除底层外）上按了下楼的请求时，unit1将调用该过程。 //unit3在实现该过程时负责记录下请求所来自的楼层，并进行相应处理。 //参数：LayerNumber表示该请求所来自的楼层（2..MAX\_LAYER） |

<!-- Slide number: 22 -->
# 算法设计问题
要求：给出核心算法的设计。
俄罗斯方块：给出方块策略；并说明不断优化调整的策略。
形式：NS图，或者伪代码。不可粘贴大量源代码。

<!-- Slide number: 23 -->
# 伪代码实例
procedure max(a1, a2, …., an: integers)
    max := a1
    for i := 2 to n
         if max < ai then max := ai
    return max{max is the largest element}

<!-- Slide number: 24 -->
# 伪代码实例
   procedure binary search(x: integer, a1,a2,…, an: i升序整数列)
    i := 1 {i 是搜索区间的左界}
    j := n {j 是搜索区间的右界}
    while i < j
           m := ⌊(i + j)/2⌋
           if x > am then i := m + 1
           else j := m
     if x = ai then location := i
     else location := 0
     return location{location是列表中与x相等元素的下标, 或者是 0}

<!-- Slide number: 25 -->